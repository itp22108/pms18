- name: Install Grafana container
  docker_container:
    name: grafana
    image: grafana/grafana
    ports:
      - "3000:3000"
    volumes:
      - "/home/srvuser:/var/lib/grafana"
    restart_policy: unless-stopped
