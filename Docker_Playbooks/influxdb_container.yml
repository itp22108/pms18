- name: Install InfluxDB container
  docker_container:
    name: influxdb
    image: influxdb:2.0
    ports:
      - "8086:8086"
    volumes:
      - "/home/srvuser:/var/lib/influxdb2"
    restart_policy: unless-stopped
